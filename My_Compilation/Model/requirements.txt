# Project ARJUNA (SIH 26170) — Model training/inference runtime (PINNED).
# MUST match the root requirements.txt so the serialized Isolation Forest model
# (Model/isolation_forest_model.joblib) is reproducible across local/CI/Docker.
scikit-learn==1.9.0
pandas==2.3.3
numpy==2.4.6
scipy==1.17.0
joblib==1.5.3
pytest==9.0.3
