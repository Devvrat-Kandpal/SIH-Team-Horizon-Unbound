# Project ARJUNA (SIH 26170) -- PINNED, reproducible runtime (audit P1-14).
# These EXACT versions are canonical: local, CI, Docker, model training,
# and model inference must all use the same set to guarantee serialized
# scikit-learn model compatibility (P-07). scipy is a direct import in
# Backend/isolation_forest.py so it is listed explicitly (P1-13).
fastapi==0.136.0
uvicorn[standard]==0.48.0
websockets==16.0
scikit-learn==1.9.0
pandas==2.3.3
numpy==2.4.6
scipy==1.17.0
joblib==1.5.3
anyio==4.12.1
pytest==9.0.3
httpx==0.28.1
python-dotenv==1.2.2
# Optional: live Supabase persistence (not required for the local demo).
supabase>=2.0,<3.0
